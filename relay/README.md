# Relay Area

Temporary code/text relay payloads belong only on short-lived `relay/*` branches following `workspace/TEMPORARY_RELAY_POLICY.md`.

Do not merge relay payloads into Workspace `main` or `develop`. Transfer coherent changes to the real owning repository and validate there.
